# Test Results

One subfolder per test run, named `YYYY-MM-DD-<short-description>/`, containing:

- Screenshots: Opta Serial Monitor, Mosquitto terminal, Modbus emulator.
- `notes.md` — outcome, any deviations from the expected result, follow-up items.
